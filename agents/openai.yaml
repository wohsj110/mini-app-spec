interface:
  display_name: "Mini-App Spec"
  short_description: "Create, calibrate, annotate, and reconcile a self-contained single-file executable Mini-App spec"
  default_prompt: "Use $mini-app-spec to create, calibrate, annotate, or reconcile a self-contained single-file executable spec from the available inputs."
