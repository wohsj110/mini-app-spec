{
  "specId": "watermark-toggle",
  "_fixtureNote": "Hand-crafted simulation of a browser annotation export. In the real flow, targetId is generated by click-to-anchor on the page and naturally matches the artifact; before running eval-3, replace the targetIds below with the short IDs from eval-2's actual artifact, otherwise REF-01 blocks the merge (which is itself the pipeline working correctly).",
  "annotations": [
    {
      "targetId": "SCN-export-off",
      "comment": "Exporting with the watermark off isn't covered when low-end devices take the degraded export pipeline",
      "proposal": "Add a \"low-end device degraded export pipeline\" branch to examples; the then clause asserts the final image has no watermark all the same"
    },
    {
      "targetId": "ST-settings",
      "comment": "Add a \"Pro exclusive\" badge next to the toggle"
    }
  ]
}
