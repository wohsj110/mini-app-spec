{
  "description": "assert 类 run 的 testCount=0（零测试匹配不得算绿）",
  "base": "valid-typical.json",
  "expect": ["ALG-05"],
  "ops": [
    { "op": "replace", "path": "/ledger/runs/0/testCount", "value": 0 }
  ]
}
