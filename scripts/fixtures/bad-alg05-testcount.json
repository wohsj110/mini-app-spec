{
  "description": "assert-kind run with testCount=0 (zero matched tests must not count as green)",
  "base": "valid-typical.json",
  "expect": [
    "ALG-05"
  ],
  "ops": [
    {
      "op": "replace",
      "path": "/ledger/runs/0/testCount",
      "value": 0
    }
  ]
}
