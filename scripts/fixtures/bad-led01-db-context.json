{
  "description": "after switching the scenario to db verification, the run lacks buildVariant/devicePackage/deviceSerial device context",
  "base": "valid-typical.json",
  "expect": [
    "LED-01"
  ],
  "ops": [
    {
      "op": "replace",
      "path": "/contract/scenarios/0/verification/kind",
      "value": "db"
    },
    {
      "op": "replace",
      "path": "/contract/scenarios/0/verification/assertion",
      "value": {
        "query": "SELECT count(*) FROM ai_cloud_task WHERE status=2",
        "predicate": ">=1"
      }
    }
  ]
}
