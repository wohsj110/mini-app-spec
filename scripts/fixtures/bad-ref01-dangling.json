{
  "description": "flow.stateRefs references the nonexistent ST-ghost",
  "base": "valid-minimal.json",
  "expect": [
    "REF-01"
  ],
  "ops": [
    {
      "op": "replace",
      "path": "/contract/flows/0/stateRefs",
      "value": [
        "ST-only",
        "ST-ghost"
      ]
    }
  ]
}
